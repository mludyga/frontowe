export type TailMode = 'auto' | 'manual';

export type TailManualLabels = {
  vertical?: string; // Pion
  diagonal?: string; // Przekątna (duża)
  base?: string;     // Podstawa
  omega?: string;    // Omega
  support?: string;  // Wspornik
};
